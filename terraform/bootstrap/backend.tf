# Using local state during initial development. Migrate to GCS backend for production.
# terraform {
#   backend "gcs" {
#     # bucket = "terraform-state-your-project-id"
#     prefix = "adk-docker-uv/bootstrap"
#   }
# }
